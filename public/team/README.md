Drop team photos here with these exact filenames:

  aravinth.jpg
  ezrajid.jpg
  kanagavel.jpg

That's it — no code changes needed. The site already looks for these
files (see `photo` field in src/data/siteData.ts) and will show them
automatically. Until a file exists, that member's card just shows the
initials placeholder instead.

Tips:
- Portrait orientation, roughly 3:4 (e.g. 900x1200px) looks best.
- Keep each file under ~500KB so the page stays fast.
- .jpg, .png or .webp all work — just update the extension in
  siteData.ts if you use something other than .jpg.
